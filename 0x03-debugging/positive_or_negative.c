#include "main.h"

/**
 * positive_or_negative - identifies the input which is positive or negative
 * @i: represents inputs 
 * Return: always void
 */

void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negative\n", i);
	else 
		printf("%d is zero\n", i);
}
